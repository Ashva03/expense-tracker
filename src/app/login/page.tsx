import React from 'react';
import Login from '../components/login/login';

export default async function LoginPage() {
  return (
    <div>
      <Login sliderlist1={[]} sliderlist2={[]} sliderlist3={[]} />
    </div>
  );
}
